import UIKit

/// Contextual information provided to a `ModalPresentationStyle` when getting display preferences.
public struct ModalBehaviorContext {
    /// Create a preference context.
    public init() {}
}
