import Foundation


/// Contextual information provided to a `ToastPresentationStyle` when getting display preferences.
public struct ToastBehaviorContext {
    /// Create a preference context.
    public init() {}
}
